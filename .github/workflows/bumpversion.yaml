on:
  workflow_dispatch:

jobs:
  bumpversion:
    runs-on: ubuntu-latest
    steps:
      - name: bumpversion
        uses: ./
        id: bumpversion
        with:
          gitEmail: sarusethi@rocketmail.com
          gitName: Gurleen Sethi
          incrementType: minor
